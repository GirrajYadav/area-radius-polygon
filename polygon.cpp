#include <iostream>
#include <math.h>
int main() {
   float r = 4;
   float a = 12;
   float area = ((r * r * a) * sin((360 / n) * 3.14159 / 180)) / 2;
   std::cout<<"area="<<area;
   return 0;
}
